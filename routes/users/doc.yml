endpoints:
  create:
    summary: Create Users
    tags:
      - Users
    description: 
      It will create new user.
    requestBody:
      description: "Request body description"
    requestHeaders:
      -
        name: "x-header"
        description: "Header description"
      -
        name: "Authorization"
        description: "Auth Header description"
    requestModels:
      "application/json": "UserRequest"
    
    methodResponses:
      -
        statusCode: '200'
        responseModels:
          "application/json": UserResponse        
  list:
    summary: "List Users"
    description: "List all the Users for matching criteria"
    tags: 
      - "Users"
    consumes:
      - "application/json"
    produces:
      - "application/json"
    methodResponses:
      -
        statusCode: '200'
        responseModels:
          "application/json": UserResponse  
  get:
    summary: "Get user"
    description: "Get single user by ID"
    tags: 
      - "Users"
    consumes:
      - "application/json"
    produces:
      - "application/json"
  update:
    summary: "Update User"
    description: "Updates a user by ID and content in the request"
    tags: 
      - "Users"
    consumes:
      - "application/json"
    produces:
      - "application/json"
  delete:
    summary: "Delete User"
    description: "Deletes a user by ID"
    tags: 
      - "Users"
    consumes:
      - "application/json"
    produces:
      - "application/json"
    