components:
  schemas:
    MemberModel:
      type: object
      properties:
        tripId:
          type: string
        memberId:
          type: string
        isFavorite:
          type: boolean
          description: if `true` then User has saved this trip for later approach
          enum:
            - true
            - false
        isMember:
          type: boolean
          description: Is member of the trip, if `true` then counted as active member of the trip
          enum:
            - true
            - false
        isActive:
          type: boolean
          description: If `isMember=true` then this value applies
          enum:
            - true
            - false
        isOwner:
          type: boolean
          description: Member is owner of the trip or not
          enum:
            - true
            - false
        updatedAt:
          type: number
          format: Unix timestamp
          description: when last time user interacted with the trip
