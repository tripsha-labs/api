components:
  schemas:
    MyTripModel:
      type: object
      properties:
        tripId:
          type: string
        memberId:
          type: string
        createdAt:
          type: string
          format: utc datetime
        isFavorite:
          type: boolean
        isOwner:
          type: boolean
        memberStatus:
          type: string
