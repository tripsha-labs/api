components:
  schemas:
    pathUserId:
      name: 'userId'
      in: 'path'
      description: 'Id of the user '
      required: true
      type: 'string'
    pathTripId:
      name: 'tripId'
      in: 'path'
      description: 'Id of the trip '
      required: true
      type: 'string'
