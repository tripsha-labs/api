components:
  parameters:
    pathUserId:
      name: 'userId'
      in: 'path'
      description: 'Id of the user'
      required: true
      schema:
        type: 'string'
    pathTripId:
      name: 'tripId'
      in: 'path'
      description: 'Id of the trip'
      required: true
      schema:
        type: 'string'
    pathHostRequestId:
      name: 'hostRequestId'
      in: 'path'
      description: 'Id of the host request'
      required: true
      schema:
        type: 'string'

    pathBookingId:
      name: 'bookingId'
      in: 'path'
      description: 'Id of the booking request'
      required: true
      schema:
        type: 'string'
