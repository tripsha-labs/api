components:
  parameters:
    pathUserId:
      name: 'userId'
      in: 'path'
      description: 'Id of the user'
      required: true
      schema:
        type: 'string'
    pathTripId:
      name: 'tripId'
      in: 'path'
      description: 'Id of the trip'
      required: true
      schema:
        type: 'string'
