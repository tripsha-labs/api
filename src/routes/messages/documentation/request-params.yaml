components:
  schemas:
    SendMessage:
      required:
        - toMemberId
        - message
      properties:
        toMemberId:
          type: string
        message:
          type: string
        messsageType:
          type: string
          enum:
            - text
  parameters:
    SendMessage:
      in: path
      name: body
      description: Create trip payload
      required: true
      schema:
        $ref: '#/components/schemas/SendMessage'
