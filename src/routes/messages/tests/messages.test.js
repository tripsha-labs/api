import { MessageController } from '../message.ctrl';
import { expect } from 'chai';
jest.mock('../../../utils/db-connector');

describe('List Messsages', () => {
  test('With valid input', async () => {});
});
